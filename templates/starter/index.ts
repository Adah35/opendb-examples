import {createIntegration, ServiceAuth} from '@opendashboard/integration-core';


export default createIntegration({
    name: 'Starter',
    identifier: 'starter',
    description: 'starter file',
    auth:null,
    actions: {
    },
    triggers: {
    },
});
